package DataStructure;

public class Hash {
	
	/*
	 
	 	해시 함수
	 	-> 문자열을 받아서 숫자를 반환하는 함수이다.
	 	즉 받은 문자열에 대해서 특정 숫자를 할당하고 이와 연관된 연산을 진행한다.
	 	
	 	ex) 사과:0, 아보카도:1, 오렌지:2
	 	
	 	int[] list = {apple, abocado, orange};
	 	만약 사과가 input으로 들어온다면 list[0] 값을 반환하는 식
	 	
	 	
	  
	 */
}
